<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fresh.freshgrouponsystem.mapper.OrderItemMapper">

    <insert id="addOrderItem">
        insert into `order_item` (order_id, product_id, product_name, product_price, quantity, create_time) values (#{orderId}, #{productId}, #{productName}, #{productPrice}, #{quantity}, NOW())
    </insert>

    <select id="getOrderItemsByOrderId" resultType="OrderItem">
        select * from `order_item` where order_id = #{orderId}
    </select>
</mapper>